export const metadata = {
  title: 'DX360 - Smart Car System',
  description: 'DX360 smart car system features and specifications'
}

export default function DX360Layout({ children }) {
  return <>{children}</>
}
